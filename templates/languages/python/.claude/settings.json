{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write(*.py)",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ruff format $CLAUDE_FILE_PATH && uv run ruff check --fix $CLAUDE_FILE_PATH"
          }
        ]
      },
      {
        "matcher": "Edit(*.py)",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ruff format $CLAUDE_FILE_PATH && uv run ruff check --fix $CLAUDE_FILE_PATH"
          }
        ]
      }
    ]
  }
}
