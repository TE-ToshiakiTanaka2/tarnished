{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "if": "Write(*.py)",
            "command": "f=\"$(jq -r '.tool_input.file_path')\" && uv run ruff format \"$f\" && uv run ruff check --fix \"$f\""
          },
          {
            "type": "command",
            "if": "Edit(*.py)",
            "command": "f=\"$(jq -r '.tool_input.file_path')\" && uv run ruff format \"$f\" && uv run ruff check --fix \"$f\""
          }
        ]
      }
    ]
  }
}
