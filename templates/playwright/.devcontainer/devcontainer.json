{
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-playwright.playwright"
            ],
            "settings": {}
        }
    },
    "postCreateCommand": "npx playwright install --with-deps chromium || true"
}
